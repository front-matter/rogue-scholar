name: Update Rogue Scholar Records
on:
  workflow_dispatch:
  # schedule:
  #  - cron: "*/10 * * * *"
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Update posts with no content_html
        run: |
          curl -X POST https://api.rogue-scholar.org/posts?blog_slug=cameronneylon&no_fulltext=true&per_page=50 -H "Authorization: Bearer ${{ secrets.SUPABASE_KEY }}"
